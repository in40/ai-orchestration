## Qwen Added Memories
- IT Lead MCP server MUST use PostgreSQL for task storage, NOT SQLite! The database is PostgreSQL with host=127.0.0.1, port=5432, database=mcp_registry, user=postgres, password=postgres. SQLite database (mcp_registry.db) should not be used for IT Lead task storage.
